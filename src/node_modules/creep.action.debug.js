const ActionNode = require('tree.node.action')

class Debug extends ActionNode {
  tick (context) {
    // var creep = context.actor

    this.log(context, 'Need a place to put energy')

    return FAILURE
  }
}

module.exports = Debug
