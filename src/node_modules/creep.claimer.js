/** @param {Creep} creep */
module.exports = (creep) => {
  if (typeof Game.flags.NextRoom === 'undefined') {
    return
  }

  /** @var {Flag} flag */
  var flag = Game.flags.NextRoom
  creep.moveTo(flag)
}
