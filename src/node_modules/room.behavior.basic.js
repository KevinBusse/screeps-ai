const Tree = require('tree')
const Sequence = require('tree.node.composite.sequence')

module.exports = new Tree(
  'room.basic',
  new Sequence('sequence', [
    // new UpkeepAllowsSpawning('CheckUpkeep', {upkeep: 50}),
    // new EnergyAvailable('EnergyHarvester', {min: 200}),
    new SpawnCreep('SpawnWorker', {behavior: 'worker', body: [WORK, CARRY, MOVE], max: 5}),
    new BuildExtensions('ExtensionBuilder', {debug: false})
  ])
)

