
module.exports = {
    status: function() {
        for (var name in Game.rooms) {

            var room = Game.rooms[name];
            console.log(name+':'+room.energyAvailable+'/'+room.energyCapacityAvailable+' '+room.find(FIND_MY_CREEPS).length +' vs. '+room.find(FIND_HOSTILE_CREEPS).length );

        }
    }
};