const ActionNode = require('tree.node.action')

class IsEnergyCapped extends ActionNode {
  tick (context) {
    var room = context.actor
    if (room.energyAvailable === room.energyCapacityAvailable) {
      return SUCCESS
    }

    return FAILURE
  }
}

module.exports = IsEnergyCapped
