module.exports = {
  /** @param {Creep} creep **/
  run: function(creep) {
    if (typeof Game.flags.NextRoom === 'undefined') {
      return;
    }

    /** @var {Flag} flag */
    var flag = Game.flags.NextRoom;
    creep.moveTo(flag);
  }
};