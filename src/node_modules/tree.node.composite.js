const Node = require('tree.node')

class CompositeNode extends Node {
  constructor (id, nodes, params) {
    super(id)
    this.nodes = nodes || []
    this.params = params
  }
}

module.exports = CompositeNode
